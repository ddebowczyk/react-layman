/**
 * Renders the resize-handle overlay for every floating window. Mounted once
 * (in `Layman.tsx`), above the tiled layout.
 */
export declare function FloatingResizeHandleLayer(): import("react").JSX.Element;
