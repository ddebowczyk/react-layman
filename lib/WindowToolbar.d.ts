import { ToolBarProps } from './types';
export declare function WindowToolbar({ windowId, path, position: rawPosition, tabs, selectedTabId, zIndex: floatingZIndex }: ToolBarProps): import("react").JSX.Element;
